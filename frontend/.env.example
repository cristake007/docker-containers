# Defaults to a same-origin relative path, which is correct once the
# frontend and backend are served through the same host nginx (see
# deploy/nginx/). Only override this if the API lives on a different origin.
VITE_API_URL=/api
