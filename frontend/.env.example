# Both default to same-origin relative paths, which is correct once the
# frontend and backend are served through the same host nginx (see
# deploy/nginx/). Only override these if the API lives on a different origin.
VITE_GRAPHQL_URL=/api/graphql
VITE_API_URL=/api
